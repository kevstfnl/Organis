model Enterprise {
    id        Int       @id @default(autoincrement())
    name      String
    reason   String
    siret     Int       @unique
    events    Event[]
    employees Employe[]
    materials Material[]
    createdAt DateTime  @default(now())
}
