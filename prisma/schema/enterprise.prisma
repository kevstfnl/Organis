model Enterprise {
    id        Int       @id @default(autoincrement())
    name      String
    reason   String
    siret     Int       @unique
    events    Event[]
    users User[]
    materials Material[]
    createdAt DateTime  @default(now())
}
